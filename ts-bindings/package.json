{
  "name": "traykit-bindings",
  "version": "0.1.3",
  "description": "Zig-powered macOS tray (menu bar) bindings for Bun (Bun-only).",
  "module": "index.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./index.ts"
    }
  },
  "files": [
    "index.ts",
    "rpc.ts",
    "model.ts",
    "README.md",
    "bin/"
  ],
  "scripts": {
    "build-binary": "cd ../binary && zig build -Doptimize=ReleaseSmall && mkdir -p ../ts-bindings/bin && cp zig-out/bin/zig-traykit ../ts-bindings/bin/zig-traykit",
    "test": "bun test",
    "prepublishOnly": "npm run build-binary && npm test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
